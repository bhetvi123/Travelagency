1.Write code that enhances all arrays such that you can call the array.last() method on any array and it will return the last element. If there are no elements in the array, it should return -1.
You may assume the array is the output of JSON.parse.

Example 1:

Input: nums = [null, {}, 3]
Output: 3
Explanation: Calling nums.last() should return the last element: 3.
Example 2:

Input: nums = []
Output: -1
Explanation: Because there are no elements, return -1.

===

console.log("Try programiz.pro");

// Add last() method to all arrays
Array.prototype.last = function() {
  return this.length ? this[this.length - 1] : -1;
};

// Example 1
let nums1 = JSON.parse('[null, {}, 5]');
console.log(nums1.last()); // Output: 3

// Example 2
let nums2 = JSON.parse('[]');
console.log(nums2.last()); // Output: -1


2.Find the first non-repeating character in a string.
Given a string, return the first character that does not repeat.
If all characters repeat, return "_".
The function should be case-sensitive ("a" ≠ "A").

===

3.Find the length of the longest substring without repeating characters.
Input: a string s.
Output: an integer (the max length of a substring with all unique characters).






1.4️⃣ Remove Duplicates from Array of Objects

Question:
Given an array of objects, remove duplicates based on a property (e.g., id).

function removeDuplicates(arr, key) {
  const seen = new Set();
  return arr.filter(item => {
    if (seen.has(item[key])) return false;
    seen.add(item[key]);
    return true;
  });
}

// Example:
const users = [
  { id: 1, name: "John" },
  { id: 2, name: "Jane" },
  { id: 1, name: "John" }
];
console.log(removeDuplicates(users, "id")); // [{id:1,...}, {id:2,...}]


2.
Given an asynchronous function fn and a time t in milliseconds, return a new time limited version of the input function. fn takes arguments provided to the time limited function.
The time limited function should follow these rules:
If the fn completes within the time limit of t milliseconds, the time limited function should resolve with the result.
If the execution of the fn exceeds the time limit, the time limited function should reject with the string "Time Limit Exceeded".




function timeLimit(fn, t) {
  return async function(...args) {
    return new Promise((resolve, reject) => {
      // Timeout that rejects after t milliseconds
      const timer = setTimeout(() => reject("Time Limit Exceeded"), t);

      // Call the original function
      fn(...args)
        .then(result => {
          clearTimeout(timer); // clear timeout if fn resolves in time
          resolve(result);
        })
        .catch(err => {
          clearTimeout(timer); // clear timeout if fn rejects
          reject(err);
        });
    });
  };
}



async function exampleFn(ms, value) {
  return new Promise(resolve => setTimeout(() => resolve(value), ms));
}

const limitedFn = timeLimit(exampleFn, 1000);

limitedFn(500, "Success").then(console.log).catch(console.log); // "Success"
limitedFn(1500, "Too Slow").then(console.log).catch(console.log); // "Time Limit Exceeded"


3.
Given an integer array arr and a filtering function fn, return a filtered array filteredArr.
The fn function takes one or two arguments:
arr[i] - number from the arr
i - index of arr[i]
filteredArr should only contain the elements from the arr for which the expression fn(arr[i], i) evaluates to a truthy value. A truthy value is a value where Boolean(value) returns true.
Please solve it without the built-in Array.filter method.


function filterArray(arr, fn) {
  const result = [];
  for (let i = 0; i < arr.length; i++) {
    if (fn(arr[i], i)) {  // check if function returns truthy
      result.push(arr[i]); // include element in result
    }
  }
  return result;
}


const arr = [1, 2, 3, 4, 5, 6];
const fn = (x, i) => x % 2 === 0; // keep only even numbers

console.log(filterArray(arr, fn)); // [2, 4, 6]

============================================================================================


<section class="pt-1 pb-1 bg-white relative">
  <div class="max-w-7xl mx-auto px-6 lg:px-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-x-20 relative">

    <!-- Card 1 -->
    <div class="relative group bg-white rounded-3xl shadow-xl p-6 flex flex-col justify-between hover:shadow-2xl transition">
      
      <!-- Shared Decorative Image (hidden initially, slide in on hover) -->
      <img src="icons/section2_3.png" 
           alt="Decor" 
           class="absolute -bottom-10 -left-10 w-40 h-auto opacity-0 translate-x-[-20px] group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-500 z-0">
      
      <img src="icons/section2_1.png" alt="Weather" class="w-auto h-auto rounded-full mx-auto relative z-10">
      <h3 class="text-xl font-semibold text-gray-900 mb-3 relative z-10">Calculated Weather</h3>
      <p class="text-gray-700 mb-6 relative z-10">Built Wicket longer admire do barton vanity itself do in it.</p>
      <button class="border border-gray-400 text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-800 hover:text-white transition relative z-10">
        Learn More
      </button>
    </div>

    <!-- Card 2 -->
    <div class="relative group bg-white rounded-3xl shadow-xl p-6 flex flex-col justify-between hover:shadow-2xl transition">
      <img src="icons/section2_3.png" 
           alt="Decor" 
           class="absolute -bottom-10 -left-10 w-40 h-auto opacity-0 translate-x-[-20px] group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-500 z-0">

      <img src="icons/section2_2.png" alt="Flights" class="w-auto h-auto rounded-full mx-auto relative z-10">
      <h3 class="text-xl font-semibold text-gray-900 mb-3 relative z-10">Best Flights</h3>
      <p class="text-gray-700 mb-6 relative z-10">Engrossed listening. Park gate sell they west hard for the.</p>
      <button class="border border-gray-400 text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-800 hover:text-white transition relative z-10">
        Learn More
      </button>
    </div>

    <!-- Card 3 -->
    <div class="relative group bg-white rounded-3xl shadow-xl p-6 flex flex-col justify-between hover:shadow-2xl transition">
      <img src="icons/section2_3.png" 
           alt="Decor" 
           class="absolute -bottom-10 -left-10 w-40 h-auto opacity-0 translate-x-[-20px] group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-500 z-0">

      <img src="icons/section2_3.png" alt="Events" class="w-auto h-auto rounded-full mx-auto relative z-10">
      <h3 class="text-xl font-semibold text-gray-900 mb-3 relative z-10">Local Events</h3>
      <p class="text-gray-700 mb-6 relative z-10">Barton vanity itself do in it. Preferd to men it engrossed listening.</p>
      <button class="border border-gray-400 text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-800 hover:text-white transition relative z-10">
        Learn More
      </button>
    </div>

    <!-- Card 4 -->
    <div class="relative group bg-white rounded-3xl shadow-xl p-6 flex flex-col justify-between hover:shadow-2xl transition">
      <img src="icons/section2_3.png" 
           alt="Decor" 
           class="absolute -bottom-10 -left-10 w-40 h-auto opacity-0 translate-x-[-20px] group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-500 z-0">

      <img src="icons/section2_4.png" alt="Customize" class="w-auto h-auto rounded-full mx-auto relative z-10">
      <h3 class="text-xl font-semibold text-gray-900 mb-3 relative z-10">Customization</h3>
      <p class="text-gray-700 mb-6 relative z-10">We deliver outsourced aviation services for military customers.</p>
      <button class="border border-gray-400 text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-800 hover:text-white transition relative z-10">
        Learn More
      </button>
    </div>

  </div>
</section>


        <!-- Rectangle behind 2nd box -->
        <img 
          src="icons/Rectangle.png" 
          alt="Decor"
          class="absolute top-60 left-[25%] w-40 h-auto ">